import React, { useState } from "react";

const News = () => {
  const [hits, setHits] = useState([]);
  return <div>{hits.length > 0 && hits.map((item) => (
    
  ))}</div>;
};

export default News;
